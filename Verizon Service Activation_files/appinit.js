// Where context = id || jQuery object || undefined.
function appInit(context) {

	// initialize app specific components/widgets/utilities
	//codeSampleInit(context);
	//smoothScrollInit(context);
    stepsInit();
}